<?xml version="1.0" encoding="utf-8"?>
<!-- 底部弹窗 / BottomSheet：毛玻璃 + 顶部 20dp 圆角 + 发丝线 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/glassFillElevated" />
    <corners
        android:topLeftRadius="@dimen/radius_sheet"
        android:topRightRadius="@dimen/radius_sheet" />
    <stroke
        android:width="@dimen/glass_hairline"
        android:color="@color/glassStroke" />
</shape>
